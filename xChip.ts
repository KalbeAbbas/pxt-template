//% color=#444444 icon="\f0b2"
namespace xChip {
    //%blockId=xChip_begin
    //%block="xChip begin"
    //%shim=xChip::begin
    export function begin(var_: number): number {
        // return is for simulator only. return of cpp code will run on hardware.
        return 1;
    }
}
